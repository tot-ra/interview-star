# JavaScript / TypeScript - Ответы на вопросы интервью

## 📚 Теория

### 1. Event Loop

**Вопрос:** Объясните, как работает Event Loop в JavaScript. Что такое Call Stack, Task Queue и Microtask Queue?

**Ответ:**

Event Loop — это механизм, который позволяет JavaScript выполнять асинхронные операции, несмотря на однопоточность.

**Компоненты:**

- **Call Stack** — стек вызовов, где выполняются синхронные операции. LIFO (Last In, First Out).
- **Web APIs** — браузерные API (setTimeout, fetch, DOM events), которые обрабатываются вне основного потока.
- **Task Queue (Macrotask Queue)** — очередь для колбэков setTimeout, setInterval, I/O, UI rendering.
- **Microtask Queue** — очередь для Promise callbacks, queueMicrotask, MutationObserver.

**Порядок выполнения:**
1. Выполняется весь синхронный код (Call Stack)
2. После каждой задачи из Call Stack — выполняются ВСЕ microtasks
3. Затем берется ОДНА задача из Task Queue
4. Повторяем с пункта 2

**Пример:**
```javascript
console.log('1');           // Sync
setTimeout(() => console.log('2'), 0);  // Macrotask
Promise.resolve().then(() => console.log('3'));  // Microtask
console.log('4');           // Sync
// Вывод: 1, 4, 3, 2
```

**Ключевой момент:** Microtasks всегда выполняются раньше macrotasks, даже если macrotask был добавлен первым.

---

### 2. Closures (Замыкания)

**Вопрос:** Что такое замыкания (closures) и как они работают? Приведите практический пример использования.

**Ответ:**

**Замыкание** — это функция, которая запоминает свое лексическое окружение (переменные из внешней области видимости), даже когда выполняется вне этой области.

**Как работает:**
- При создании функции она "захватывает" ссылки на переменные из внешнего scope
- Эти переменные не удаляются сборщиком мусора, пока существует замыкание

**Практические применения:**

1. **Приватные переменные (инкапсуляция):**
```javascript
function createCounter() {
  let count = 0;  // Приватная переменная
  return {
    increment: () => ++count,
    getCount: () => count
  };
}
const counter = createCounter();
counter.increment(); // 1
counter.count; // undefined — нет доступа напрямую
```

2. **Фабрики функций:**
```javascript
function multiply(a) {
  return (b) => a * b;
}
const double = multiply(2);
double(5); // 10
```

3. **Сохранение состояния в обработчиках событий:**
```javascript
function setupButton(buttonId, message) {
  document.getElementById(buttonId).addEventListener('click', () => {
    alert(message);  // message захвачен замыканием
  });
}
```

**Частая ошибка** — замыкание в цикле с `var`:
```javascript
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 100);
}
// Выведет: 3, 3, 3 (не 0, 1, 2)
// Решение: использовать let или IIFE
```

---

### 3. Prototypes (Прототипное наследование)

**Вопрос:** Объясните прототипное наследование в JavaScript. Чем оно отличается от классического ООП?

**Ответ:**

**Прототипное наследование** — объекты наследуют напрямую от других объектов через цепочку прототипов.

**Как работает:**
- Каждый объект имеет скрытое свойство `[[Prototype]]` (доступно через `__proto__` или `Object.getPrototypeOf`)
- При обращении к свойству JS ищет его сначала в объекте, затем по цепочке прототипов
- Цепочка заканчивается на `Object.prototype`, у которого `[[Prototype]]` = null

**Способы создания наследования:**

```javascript
// 1. Object.create
const animal = { eat() { console.log('eating'); } };
const dog = Object.create(animal);
dog.bark = function() { console.log('woof'); };

// 2. Конструкторы (до ES6)
function Animal(name) { this.name = name; }
Animal.prototype.eat = function() { console.log('eating'); };

function Dog(name) { Animal.call(this, name); }
Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;

// 3. ES6 классы (синтаксический сахар над прототипами)
class Animal { eat() { console.log('eating'); } }
class Dog extends Animal { bark() { console.log('woof'); } }
```

**Отличия от классического ООП:**

| Классическое ООП | Прототипное наследование |
|------------------|-------------------------|
| Классы — шаблоны | Объекты наследуют от объектов |
| Копирование при создании | Делегирование через цепочку |
| Жесткая иерархия | Гибкая, динамическая |
| Множественное наследование сложно | Можно смешивать (mixins) |

---

### 4. Контекст this

**Вопрос:** Как работает ключевое слово `this` в разных контекстах?

**Ответ:**

`this` определяется **в момент вызова** функции, а не её создания (кроме стрелочных функций).

**Правила определения this:**

| Контекст | Значение this |
|----------|---------------|
| Глобальный (strict mode) | `undefined` |
| Глобальный (non-strict) | `window` / `global` |
| Метод объекта | Объект перед точкой |
| Конструктор (new) | Новый создаваемый объект |
| call/apply/bind | Явно переданный объект |
| Стрелочная функция | Лексический this (из окружения) |
| Event handler | Элемент, на котором сработало событие |

**Примеры:**

```javascript
const obj = {
  name: 'Object',
  regular: function() { console.log(this.name); },
  arrow: () => { console.log(this.name); }
};

obj.regular();  // 'Object' — метод объекта
obj.arrow();    // undefined — стрелочная берет this из глобального scope

const fn = obj.regular;
fn();           // undefined — потеря контекста

// Решения потери контекста:
fn.call(obj);           // 'Object'
fn.bind(obj)();         // 'Object'
obj.regular.bind(obj);  // Сохранить привязку
```

**Стрелочные функции:**
- Не имеют своего `this`
- Берут `this` из лексического окружения (где были созданы)
- Нельзя изменить через call/apply/bind

---

### 5. Promises

**Вопрос:** Объясните различия между `Promise.all()`, `Promise.race()`, `Promise.any()` и `Promise.allSettled()`.

**Ответ:**

| Метод | Резолвится когда | Реджектится когда | Результат |
|-------|------------------|-------------------|-----------|
| `Promise.all` | Все fulfilled | Первый rejected | Массив результатов |
| `Promise.race` | Первый settled | Первый rejected (если он первый) | Результат первого |
| `Promise.any` | Первый fulfilled | Все rejected | Результат первого успешного |
| `Promise.allSettled` | Все settled | Никогда | Массив {status, value/reason} |

**Примеры использования:**

```javascript
const p1 = Promise.resolve(1);
const p2 = Promise.resolve(2);
const p3 = Promise.reject('error');

// Promise.all — "всё или ничего"
Promise.all([p1, p2]).then(console.log);     // [1, 2]
Promise.all([p1, p3]).catch(console.log);    // 'error'

// Promise.race — первый результат (успех или ошибка)
Promise.race([p1, p2]).then(console.log);    // 1

// Promise.any — первый успех (ES2021)
Promise.any([p3, p1]).then(console.log);     // 1
Promise.any([p3]).catch(e => console.log(e)); // AggregateError

// Promise.allSettled — все результаты, без прерывания
Promise.allSettled([p1, p3]).then(console.log);
// [{status: 'fulfilled', value: 1}, {status: 'rejected', reason: 'error'}]
```

**Когда использовать:**
- `all` — параллельные запросы, где нужны все результаты
- `race` — таймауты, первый ответ из нескольких источников
- `any` — fallback между несколькими серверами
- `allSettled` — batch операции, где нужно знать результат каждой

---

### 6. TypeScript Generics

**Вопрос:** Объясните generics в TypeScript. Когда и зачем их использовать?

**Ответ:**

**Generics** — параметризованные типы, позволяющие создавать переиспользуемый код с сохранением типобезопасности.

**Зачем нужны:**
- Избежать дублирования кода для разных типов
- Сохранить информацию о типе (в отличие от `any`)
- Создавать гибкие, но типобезопасные API

**Базовый синтаксис:**

```typescript
// Функция
function identity<T>(arg: T): T {
  return arg;
}
identity<string>('hello');  // Явно
identity(42);               // TypeScript выведет number

// Интерфейс
interface Box<T> {
  value: T;
}
const stringBox: Box<string> = { value: 'hello' };

// Класс
class Queue<T> {
  private items: T[] = [];
  enqueue(item: T) { this.items.push(item); }
  dequeue(): T | undefined { return this.items.shift(); }
}
```

**Ограничения (Constraints):**

```typescript
interface HasLength {
  length: number;
}

function logLength<T extends HasLength>(arg: T): void {
  console.log(arg.length);
}

logLength('hello');    // OK
logLength([1, 2, 3]);  // OK
logLength(123);        // Error: number не имеет length
```

**Несколько параметров и значения по умолчанию:**

```typescript
function map<T, U = T>(arr: T[], fn: (item: T) => U): U[] {
  return arr.map(fn);
}

// keyof — получение ключей типа
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}
```

---

### 7. TypeScript Utility Types

**Вопрос:** Объясните и продемонстрируйте использование `Pick`, `Omit`, `Exclude`, `ReturnType`, `Partial`, `Required`.

**Ответ:**

```typescript
interface User {
  id: number;
  name: string;
  email: string;
  age?: number;
}

// Partial<T> — все свойства опциональны
type PartialUser = Partial<User>;
// { id?: number; name?: string; email?: string; age?: number; }

// Required<T> — все свойства обязательны
type RequiredUser = Required<User>;
// { id: number; name: string; email: string; age: number; }

// Pick<T, K> — выбрать только указанные свойства
type UserPreview = Pick<User, 'id' | 'name'>;
// { id: number; name: string; }

// Omit<T, K> — исключить указанные свойства
type UserWithoutEmail = Omit<User, 'email'>;
// { id: number; name: string; age?: number; }

// Exclude<T, U> — исключить типы из union
type Status = 'pending' | 'success' | 'error';
type NonErrorStatus = Exclude<Status, 'error'>;
// 'pending' | 'success'

// Extract<T, U> — извлечь только указанные типы
type OnlyError = Extract<Status, 'error' | 'warning'>;
// 'error'

// ReturnType<T> — тип возвращаемого значения функции
function getUser() { return { id: 1, name: 'John' }; }
type UserReturn = ReturnType<typeof getUser>;
// { id: number; name: string; }

// Parameters<T> — типы параметров функции
type GetUserParams = Parameters<typeof getUser>;
// []

// Record<K, V> — объект с ключами K и значениями V
type UserRoles = Record<'admin' | 'user' | 'guest', boolean>;
// { admin: boolean; user: boolean; guest: boolean; }

// NonNullable<T> — исключить null и undefined
type MaybeString = string | null | undefined;
type DefinitelyString = NonNullable<MaybeString>;
// string
```

---

### 8. Interface vs Type

**Вопрос:** В чем разница между `interface` и `type` в TypeScript? Когда использовать каждый?

**Ответ:**

**Общие возможности:**
- Описание формы объекта
- Расширение/наследование
- Использование с generics

**Различия:**

| Возможность | interface | type |
|-------------|-----------|------|
| Declaration merging | ✅ Да | ❌ Нет |
| extends | ✅ Да | ✅ Через & |
| implements | ✅ Да | ✅ Да |
| Union types | ❌ Нет | ✅ Да |
| Primitive aliases | ❌ Нет | ✅ Да |
| Tuple types | ❌ Нет | ✅ Да |
| Mapped types | ❌ Нет | ✅ Да |

**Примеры:**

```typescript
// Declaration merging (только interface)
interface User { name: string; }
interface User { age: number; }
// User = { name: string; age: number; }

// Union types (только type)
type Status = 'pending' | 'success' | 'error';
type Result = User | null;

// Tuple (только type)
type Coordinates = [number, number];

// Mapped types (только type)
type Readonly<T> = { readonly [K in keyof T]: T[K] };

// Расширение
interface Animal { name: string; }
interface Dog extends Animal { bark(): void; }

type Animal = { name: string };
type Dog = Animal & { bark(): void };
```

**Рекомендации:**
- **interface** — для публичных API, библиотек (declaration merging), ООП
- **type** — для union/intersection, алиасы примитивов, сложные типы
- В большинстве случаев взаимозаменяемы — выбирайте один стиль для проекта

---

### 9. Type Coercion (Приведение типов)

**Вопрос:** Как работает приведение типов в JavaScript? Приведите примеры явного и неявного приведения.

**Ответ:**

**Явное приведение (Explicit):**

```javascript
// К строке
String(123);        // '123'
(123).toString();   // '123'
123 + '';           // '123' (технически неявное)

// К числу
Number('123');      // 123
parseInt('123px');  // 123
parseFloat('3.14'); // 3.14
+'123';             // 123

// К булеану
Boolean(1);         // true
!!1;                // true
```

**Неявное приведение (Implicit):**

```javascript
// Строковая конкатенация
'5' + 3;            // '53' (число → строка)
'5' + null;         // '5null'

// Математические операции (кроме +)
'5' - 3;            // 2 (строка → число)
'5' * '2';          // 10
'5' / null;         // Infinity (null → 0)

// Сравнение ==
'5' == 5;           // true
null == undefined;  // true
0 == false;         // true
'' == false;        // true

// Логические операции
!!'hello';          // true
!!0;                // false
```

**Falsy значения:**
```javascript
false, 0, -0, 0n, '', null, undefined, NaN
// Всё остальное — truthy (включая '0', [], {})
```

**Особые случаи:**

```javascript
[] + [];            // '' (оба → '')
[] + {};            // '[object Object]'
{} + [];            // 0 (в консоли {} интерпретируется как блок)

null + 1;           // 1 (null → 0)
undefined + 1;      // NaN (undefined → NaN)

[1,2] + [3,4];      // '1,23,4'
```

**Рекомендация:** Используйте `===` вместо `==` для избежания неожиданного приведения.

---

### 10. Modules (CommonJS vs ES Modules)

**Вопрос:** Объясните разницу между CommonJS и ES Modules. Как работает tree-shaking?

**Ответ:**

**CommonJS (CJS):**
```javascript
// Экспорт
module.exports = { foo: 1 };
exports.bar = 2;

// Импорт
const module = require('./module');
const { foo } = require('./module');
```

**ES Modules (ESM):**
```javascript
// Экспорт
export const foo = 1;
export default function() {}

// Импорт
import module from './module';
import { foo } from './module';
import * as all from './module';
```

**Ключевые различия:**

| Аспект | CommonJS | ES Modules |
|--------|----------|------------|
| Загрузка | Синхронная | Асинхронная |
| Парсинг | В runtime | Статический (до выполнения) |
| Экспорт | Копия значения | Live binding (ссылка) |
| this на верхнем уровне | module.exports | undefined |
| Условный импорт | ✅ Да | ❌ Только dynamic import() |
| Tree-shaking | ❌ Сложно | ✅ Нативная поддержка |

**Tree-shaking:**

Процесс удаления неиспользуемого кода при сборке.

```javascript
// utils.js
export const used = () => 'used';
export const unused = () => 'unused';

// main.js
import { used } from './utils';
used();
// После tree-shaking: unused будет удалена из бандла
```

**Почему работает с ESM:**
- Статический анализ импортов/экспортов до выполнения
- Bundler (Webpack, Rollup, esbuild) видит, что используется

**Почему не работает с CJS:**
- Динамический require() — нельзя определить зависимости статически
- `module.exports` может быть изменен в runtime

**Dynamic import (общий для обоих):**
```javascript
const module = await import('./module.js');
// Работает в обоих системах, возвращает Promise
```

---

## 💻 Практика / Live Coding

### 1. Throttle и Debounce

**Задача:** Напишите реализацию `throttle` и `debounce` функций с нуля.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                      DEBOUNCE                                │
│                                                              │
│  Event ──► Event ──► Event ──► [wait] ──► Execute           │
│                                  ▲                           │
│                        Таймер сбрасывается                   │
│                        при каждом вызове                     │
│                                                              │
│  Применение: поиск при вводе, resize, auto-save             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      THROTTLE                                │
│                                                              │
│  Execute ──► [block] ──► Execute ──► [block] ──► Execute    │
│     ▲          │            ▲          │                     │
│     │          │            │          │                     │
│  Events     Ignored      Events     Ignored                  │
│                                                              │
│  Применение: scroll, mousemove, игровой loop                │
└─────────────────────────────────────────────────────────────┘
```

**Ключевые компоненты:**

**Debounce:**
- Хранить ID таймера в замыкании
- При каждом вызове: очистить предыдущий таймер, установить новый
- Опционально: `leading` (вызов в начале), `trailing` (вызов в конце)

**Throttle:**
- Хранить timestamp последнего вызова или флаг блокировки
- Вызывать функцию только если прошло достаточно времени
- Опционально: `leading`, `trailing`

**Сложность:** O(1) время и память для обеих функций.

---

### 2. Promise.all Implementation

**Задача:** Реализуйте упрощенную версию `Promise.all()`.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                    Promise.all Flow                          │
│                                                              │
│  Input: [Promise1, Promise2, Promise3]                       │
│                                                              │
│           ┌──────────┐                                       │
│  P1 ─────►│ Resolved │──┐                                    │
│           └──────────┘  │                                    │
│           ┌──────────┐  │    ┌─────────┐   ┌─────────────┐  │
│  P2 ─────►│ Resolved │──┼───►│ Counter │──►│ All Done?   │  │
│           └──────────┘  │    │ = 3     │   │ Resolve all │  │
│           ┌──────────┐  │    └─────────┘   └─────────────┘  │
│  P3 ─────►│ Resolved │──┘                                    │
│           └──────────┘                                       │
│                                                              │
│  Любой Reject ─────────────────────────► Reject all          │
└─────────────────────────────────────────────────────────────┘
```

**Ключевые компоненты:**

1. Создать новый Promise-обертку
2. Массив результатов той же длины, что и входной
3. Счетчик завершенных промисов
4. Для каждого промиса:
   - При resolve: сохранить результат по индексу, инкрементировать счетчик
   - При reject: сразу reject всего Promise.all
5. Когда счетчик = длине массива: resolve с массивом результатов

**Edge cases:**
- Пустой массив → resolve([])
- Не-промисы в массиве → обернуть в Promise.resolve()

---

### 3. Deep Clone

**Задача:** Напишите функцию глубокого клонирования объекта с обработкой циклических ссылок.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                    Deep Clone Algorithm                      │
│                                                              │
│  Input Object ──► Check Type ──► Clone Strategy              │
│                       │                                      │
│         ┌─────────────┼─────────────┐                        │
│         ▼             ▼             ▼                        │
│    Primitive      Object/Array    Special                    │
│    (return as is) (recurse)       (Date, Map, Set, etc.)    │
│                       │                                      │
│                       ▼                                      │
│              ┌─────────────────┐                             │
│              │ WeakMap Cache   │ ◄── Для циклических ссылок  │
│              │ (original→clone)│                             │
│              └─────────────────┘                             │
│                       │                                      │
│                       ▼                                      │
│              For each property:                              │
│              clone[key] = deepClone(value)                   │
└─────────────────────────────────────────────────────────────┘
```

**Ключевые компоненты:**

1. **WeakMap** для хранения уже клонированных объектов (решает циклические ссылки)
2. Проверка типа: `typeof`, `Array.isArray`, `instanceof`
3. Обработка специальных типов:
   - Date → new Date(original)
   - RegExp → new RegExp(original)
   - Map → new Map(), рекурсивно клонировать entries
   - Set → new Set(), рекурсивно клонировать values
4. Рекурсивный обход свойств объекта

**Edge cases:**
- null (typeof null === 'object')
- Symbol keys
- Non-enumerable properties
- Getters/setters

---

### 4. Flatten Array

**Задача:** Реализуйте функцию `flatten(arr, depth)` для многомерных массивов.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                    Flatten Algorithm                         │
│                                                              │
│  Input: [[1, [2, [3]]], 4]  depth: 2                        │
│                                                              │
│  Рекурсивный подход:                                         │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ flatten([1, [2, [3]]], 1)                               ││
│  │    → [1, ...flatten([2, [3]], 0)]                       ││
│  │    → [1, 2, [3]]                                        ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Итеративный подход (стек):                                  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Stack: [[item, depth], ...]                             ││
│  │ Result: []                                              ││
│  │                                                         ││
│  │ Pop item → Is Array && depth > 0?                       ││
│  │    Yes → Push children with depth-1                     ││
│  │    No  → Add to result                                  ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Два подхода:**

1. **Рекурсивный:** reduce + concat + рекурсия
   - Проще понять
   - Может быть stack overflow на глубоких массивах

2. **Итеративный со стеком:**
   - Избегает stack overflow
   - Хранит [элемент, оставшаяся глубина]

**Edge cases:**
- depth = 0 → вернуть копию массива
- depth = Infinity → полностью flatten
- Sparse arrays (holes)

---

### 5. Event Emitter

**Задача:** Реализуйте паттерн "Наблюдатель" (Observer/EventEmitter).

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                    EventEmitter Structure                    │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ events: Map<string, Set<Function>>                      ││
│  │                                                         ││
│  │ 'click' ──► [handler1, handler2, handler3]              ││
│  │ 'load'  ──► [handler4]                                  ││
│  │ 'error' ──► [handler5, handler6]                        ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Methods:                                                    │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ on(event, handler)      → Add to Set                    ││
│  │ off(event, handler)     → Remove from Set               ││
│  │ emit(event, ...args)    → Call all handlers             ││
│  │ once(event, handler)    → Add wrapper that removes self ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**API:**

```typescript
interface EventEmitter {
  on(event: string, handler: Function): void;
  off(event: string, handler: Function): void;
  emit(event: string, ...args: any[]): void;
  once(event: string, handler: Function): void;
}
```

**Ключевые компоненты:**

1. **Хранилище:** Map или объект { eventName: Set<handlers> }
2. **on:** Добавить handler в Set для события
3. **off:** Удалить handler из Set
4. **emit:** Перебрать все handlers и вызвать с аргументами
5. **once:** Обернуть handler в функцию, которая вызывает off после первого вызова

**Дополнительные возможности:**
- `removeAllListeners(event?)` — удалить все
- `listenerCount(event)` — количество слушателей
- `eventNames()` — список событий
- Обработка ошибок в handlers
