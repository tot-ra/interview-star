# Алгоритмы и Структуры Данных - Ответы на вопросы интервью

## 📚 Теория

### 1. Big O Notation

**Вопрос:** Объясните временную и пространственную сложность. Приведите примеры O(1), O(n), O(log n), O(n²).

**Ответ:**

**Big O** описывает верхнюю границу роста времени/памяти при увеличении входных данных.

**Основные сложности (от лучшей к худшей):**

| Сложность | Название | Пример | n=1000 операций |
|-----------|----------|--------|-----------------|
| O(1) | Константная | Доступ к элементу массива по индексу | 1 |
| O(log n) | Логарифмическая | Бинарный поиск | ~10 |
| O(n) | Линейная | Поиск в несортированном массиве | 1,000 |
| O(n log n) | Линейно-логарифмическая | Merge Sort, Quick Sort (avg) | ~10,000 |
| O(n²) | Квадратичная | Bubble Sort, вложенные циклы | 1,000,000 |
| O(2ⁿ) | Экспоненциальная | Рекурсивный Fibonacci | ~10³⁰⁰ |
| O(n!) | Факториальная | Перебор перестановок | ∞ |

**Примеры кода:**

```javascript
// O(1) — Константная
function getFirst(arr) {
  return arr[0];  // Всегда одна операция
}

// O(log n) — Логарифмическая
function binarySearch(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1;
}

// O(n) — Линейная
function findMax(arr) {
  let max = arr[0];
  for (const num of arr) {  // n итераций
    if (num > max) max = num;
  }
  return max;
}

// O(n²) — Квадратичная
function bubbleSort(arr) {
  for (let i = 0; i < arr.length; i++) {      // n
    for (let j = 0; j < arr.length - i; j++) { // n
      if (arr[j] > arr[j + 1]) {
        [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
      }
    }
  }
}
```

**Пространственная сложность:**
- In-place алгоритмы: O(1) дополнительной памяти
- Рекурсия: O(глубина стека)
- Merge Sort: O(n) — нужен дополнительный массив

**Правила анализа:**
- Константы отбрасываются: O(2n) = O(n)
- Берется доминирующий член: O(n² + n) = O(n²)
- Разные переменные не объединяются: O(n + m) ≠ O(n)

---

### 2. Hash Tables

**Вопрос:** Как работают хеш-таблицы? Что такое коллизии и как их разрешать?

**Ответ:**

**Хеш-таблица** — структура данных для хранения пар ключ-значение с O(1) средним временем доступа.

**Как работает:**
1. Ключ передается в **хеш-функцию** → получаем число (хеш)
2. Хеш преобразуется в индекс: `index = hash % arraySize`
3. Значение хранится в массиве по этому индексу

```
Key "name" ──► hash("name") = 12345 ──► 12345 % 10 = 5 ──► array[5]
```

**Коллизия** — когда разные ключи дают одинаковый индекс.

**Методы разрешения коллизий:**

**1. Separate Chaining (цепочки):**
```
array[5] → LinkedList: [("name", "John") → ("age", 30)]
```
- Каждая ячейка — связанный список
- При коллизии добавляем в список
- Worst case O(n) если все в одной ячейке

**2. Open Addressing (открытая адресация):**
- При коллизии ищем следующую свободную ячейку
- **Linear Probing:** index + 1, index + 2, ...
- **Quadratic Probing:** index + 1², index + 2², ...
- **Double Hashing:** index + i * hash2(key)

**Load Factor:** `n / capacity`
- При LF > 0.7 — увеличиваем размер массива (rehashing)
- Rehashing: O(n) — пересчитываем все хеши

**JavaScript реализации:**
- `Object` — строковые ключи, прототипное наследование
- `Map` — любые ключи, сохраняет порядок вставки, итерируемый
- `Set` — только ключи (без значений)

**Хорошая хеш-функция:**
- Детерминированная (одинаковый вход → одинаковый выход)
- Равномерное распределение
- Быстрое вычисление
- Минимум коллизий

---

### 3. Trees

**Вопрос:** Объясните разницу между Binary Tree, BST, AVL и Red-Black Tree. Когда какую использовать?

**Ответ:**

**Binary Tree (Бинарное дерево):**
- Каждый узел имеет максимум 2 детей (left, right)
- Нет ограничений на порядок элементов

**BST (Binary Search Tree):**
- Левое поддерево содержит значения < корня
- Правое поддерево содержит значения > корня
- Поиск, вставка, удаление: O(log n) в среднем
- **Проблема:** может выродиться в список → O(n)

```
    5           1
   / \           \
  3   7    vs     2
 / \   \           \
1   4   9           3  ← вырожденное BST
```

**AVL Tree:**
- Самобалансирующееся BST
- Разница высот поддеревьев (balance factor) ≤ 1
- После вставки/удаления — ротации для балансировки
- Гарантированно O(log n) для всех операций
- **Строже сбалансировано** → быстрее поиск, медленнее вставка/удаление

**Red-Black Tree:**
- Самобалансирующееся BST
- Узлы красные или черные
- Правила: корень черный, красный не имеет красных детей, пути имеют одинаковое число черных узлов
- **Менее строго сбалансировано** → быстрее вставка/удаление
- Используется в: TreeMap (Java), std::map (C++)

**Сравнение:**

| Операция | BST avg | BST worst | AVL | Red-Black |
|----------|---------|-----------|-----|-----------|
| Search | O(log n) | O(n) | O(log n) | O(log n) |
| Insert | O(log n) | O(n) | O(log n) | O(log n) |
| Delete | O(log n) | O(n) | O(log n) | O(log n) |
| Rotations | - | - | Больше | Меньше |

**Когда использовать:**
- **BST:** когда данные случайные, не нужна гарантия
- **AVL:** много поисков, мало вставок (словари, базы данных)
- **Red-Black:** много вставок/удалений (стандартные библиотеки)

---

### 4. Graphs

**Вопрос:** Объясните BFS и DFS. Когда какой алгоритм предпочтительнее?

**Ответ:**

**BFS (Breadth-First Search) — Поиск в ширину:**
- Исследует уровень за уровнем
- Использует **очередь (Queue)**
- Находит кратчайший путь в невзвешенном графе

```
     1
    /|\
   2 3 4     BFS: 1 → 2,3,4 → 5,6,7
  /| | |\
 5 6 7 8 9
```

**DFS (Depth-First Search) — Поиск в глубину:**
- Идет вглубь до конца, затем возвращается
- Использует **стек (Stack)** или рекурсию
- Использует меньше памяти

```
     1
    /|\
   2 3 4     DFS: 1 → 2 → 5 → 6 → 3 → 7 → 4 → 8 → 9
  /| | |\
 5 6 7 8 9
```

**Сложность:**
- Оба: O(V + E) время, где V — вершины, E — ребра
- BFS: O(V) память (очередь с целым уровнем)
- DFS: O(H) память (H — высота/глубина)

**Когда использовать BFS:**
- Кратчайший путь в невзвешенном графе
- Поиск на минимальном расстоянии
- Уровни в дереве
- Социальные сети (друзья друзей)

**Когда использовать DFS:**
- Проверка связности
- Топологическая сортировка
- Поиск циклов
- Лабиринты (все пути)
- Решение головоломок (судоку)
- Когда решение глубоко в графе

**Реализация (псевдокод):**

```javascript
// BFS
function bfs(graph, start) {
  const visited = new Set([start]);
  const queue = [start];
  
  while (queue.length) {
    const node = queue.shift();
    for (const neighbor of graph[node]) {
      if (!visited.has(neighbor)) {
        visited.add(neighbor);
        queue.push(neighbor);
      }
    }
  }
}

// DFS (итеративно)
function dfs(graph, start) {
  const visited = new Set();
  const stack = [start];
  
  while (stack.length) {
    const node = stack.pop();
    if (!visited.has(node)) {
      visited.add(node);
      for (const neighbor of graph[node]) {
        stack.push(neighbor);
      }
    }
  }
}
```

---

### 5. Sorting Algorithms

**Вопрос:** Сравните QuickSort, MergeSort и HeapSort по времени, памяти и стабильности.

**Ответ:**

| Критерий | QuickSort | MergeSort | HeapSort |
|----------|-----------|-----------|----------|
| Best | O(n log n) | O(n log n) | O(n log n) |
| Average | O(n log n) | O(n log n) | O(n log n) |
| Worst | O(n²) | O(n log n) | O(n log n) |
| Memory | O(log n) | O(n) | O(1) |
| Stable | ❌ Нет | ✅ Да | ❌ Нет |
| In-place | ✅ Да | ❌ Нет | ✅ Да |
| Cache | Отличный | Хороший | Плохой |

**QuickSort:**
- Выбрать pivot, разделить массив на < pivot и > pivot
- Рекурсивно сортировать части
- **Плюсы:** быстрый на практике, хорошая локальность кеша
- **Минусы:** O(n²) при плохом выборе pivot
- **Оптимизации:** randomized pivot, median-of-three

**MergeSort:**
- Разделить массив пополам
- Рекурсивно сортировать половины
- Слить отсортированные части
- **Плюсы:** стабильный, гарантированный O(n log n)
- **Минусы:** O(n) дополнительной памяти
- **Применение:** сортировка связанных списков, внешняя сортировка

**HeapSort:**
- Построить max-heap из массива
- Извлекать максимум, перемещая в конец
- **Плюсы:** O(1) память, гарантированный O(n log n)
- **Минусы:** плохая локальность кеша, не стабильный
- **Применение:** когда нужна гарантия и память ограничена

**Стабильность:** сохранение относительного порядка равных элементов.
```
[(a, 1), (b, 1)] → стабильная сортировка сохранит (a, 1) перед (b, 1)
```

**На практике:**
- `Array.sort()` в V8: TimSort (гибрид Merge + Insertion)
- Для небольших массивов: Insertion Sort
- Python: TimSort

---

### 6. Dynamic Programming

**Вопрос:** Объясните концепцию DP. Чем отличается memoization от tabulation?

**Ответ:**

**Dynamic Programming** — метод решения задач путем разбиения на подзадачи с сохранением результатов.

**Когда применять DP:**
1. **Оптимальная подструктура:** оптимальное решение содержит оптимальные решения подзадач
2. **Перекрывающиеся подзадачи:** одни и те же подзадачи решаются многократно

**Два подхода:**

**Memoization (Top-Down):**
- Рекурсия + кеширование
- Вычисляем только нужные подзадачи
- Проще писать, понятнее логика

```javascript
// Fibonacci с memoization
function fib(n, memo = {}) {
  if (n <= 1) return n;
  if (memo[n]) return memo[n];
  memo[n] = fib(n - 1, memo) + fib(n - 2, memo);
  return memo[n];
}
```

**Tabulation (Bottom-Up):**
- Итеративно заполняем таблицу от базовых случаев
- Вычисляем все подзадачи
- Нет overhead рекурсии, можно оптимизировать память

```javascript
// Fibonacci с tabulation
function fib(n) {
  if (n <= 1) return n;
  const dp = [0, 1];
  for (let i = 2; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2];
  }
  return dp[n];
}

// Оптимизация памяти O(1)
function fib(n) {
  if (n <= 1) return n;
  let prev2 = 0, prev1 = 1;
  for (let i = 2; i <= n; i++) {
    [prev2, prev1] = [prev1, prev2 + prev1];
  }
  return prev1;
}
```

**Сравнение:**

| Аспект | Memoization | Tabulation |
|--------|-------------|------------|
| Направление | Top-down | Bottom-up |
| Реализация | Рекурсия + кеш | Итерация + таблица |
| Вычисления | Только нужные | Все подзадачи |
| Overhead | Call stack | Нет |
| Оптимизация памяти | Сложнее | Проще |

**Классические задачи:**
- Fibonacci
- Knapsack (рюкзак)
- Longest Common Subsequence
- Coin Change
- Edit Distance
- Matrix Chain Multiplication

---

### 7. Linked Lists

**Вопрос:** В чем преимущества и недостатки связанных списков по сравнению с массивами?

**Ответ:**

**Структура:**
```
Array:  [1][2][3][4][5]  — непрерывная память

LinkedList: [1|→] → [2|→] → [3|→] → [4|→] → [5|null]
            head                              tail
```

**Сравнение операций:**

| Операция | Array | LinkedList |
|----------|-------|------------|
| Доступ по индексу | O(1) | O(n) |
| Поиск | O(n) | O(n) |
| Вставка в начало | O(n) | O(1) |
| Вставка в конец | O(1)* | O(1) с tail |
| Вставка в середину | O(n) | O(1) если есть указатель |
| Удаление из начала | O(n) | O(1) |
| Удаление из конца | O(1) | O(n) или O(1) для doubly |
| Память | Компактная | +указатели |

*amortized, при resize O(n)

**Преимущества LinkedList:**
- O(1) вставка/удаление при известной позиции
- Динамический размер без resize
- Нет перемещения элементов

**Недостатки LinkedList:**
- O(n) доступ по индексу
- Дополнительная память на указатели
- Плохая локальность кеша (элементы разбросаны в памяти)
- Нет random access

**Типы:**
- **Singly Linked List:** только next
- **Doubly Linked List:** next + prev (O(1) удаление с конца)
- **Circular:** last → first

**Когда использовать LinkedList:**
- Частые вставки/удаления в начало
- Реализация стека/очереди
- Когда размер непредсказуем
- LRU Cache (с HashMap)

**Когда использовать Array:**
- Частый доступ по индексу
- Итерация по порядку
- Важна память и кеш
- Сортировка

---

### 8. Stacks & Queues

**Вопрос:** Объясните применение стеков и очередей. Приведите реальные примеры использования.

**Ответ:**

**Stack (LIFO — Last In, First Out):**
```
push(1) → push(2) → push(3) → pop() = 3
[1] → [1,2] → [1,2,3] → [1,2]
```

**Операции:** push, pop, peek/top — все O(1)

**Применение Stack:**
- **Call Stack:** вызовы функций
- **Undo/Redo:** история действий
- **Browser History:** кнопка "назад"
- **Парсинг выражений:** проверка скобок, вычисление
- **DFS:** хранение пути
- **Backtracking:** решение лабиринтов

**Queue (FIFO — First In, First Out):**
```
enqueue(1) → enqueue(2) → enqueue(3) → dequeue() = 1
[1] → [1,2] → [1,2,3] → [2,3]
```

**Операции:** enqueue, dequeue, peek/front — все O(1)

**Применение Queue:**
- **Task Scheduling:** очередь задач
- **BFS:** обход графа
- **Print Queue:** очередь печати
- **Message Queue:** RabbitMQ, Kafka
- **Event Loop:** macrotask/microtask queues
- **Rate Limiting:** sliding window

**Специальные очереди:**

**Deque (Double-Ended Queue):**
- Вставка/удаление с обоих концов
- Применение: sliding window maximum

**Priority Queue:**
- Элементы с приоритетом
- Реализация: Heap
- Применение: Dijkstra, scheduling

**Circular Queue:**
- Эффективное использование памяти
- Применение: буферы, потоки данных

**Реализация на массиве:**
```javascript
// Queue с двумя указателями
class Queue {
  constructor() {
    this.items = [];
    this.head = 0;
  }
  enqueue(item) { this.items.push(item); }
  dequeue() { return this.items[this.head++]; }
  // Периодически очищать: this.items = this.items.slice(this.head)
}
```

---

### 9. Heap

**Вопрос:** Как работает куча (heap)? Где применяется Priority Queue?

**Ответ:**

**Heap** — полное бинарное дерево с heap property:
- **Max-Heap:** родитель ≥ детей
- **Min-Heap:** родитель ≤ детей

```
Max-Heap:        Min-Heap:
    100              1
   /   \           /   \
  19    36        2     3
 / \   / \       / \   / \
17 3  25  1     4   5  6   7
```

**Хранение в массиве:**
```
[100, 19, 36, 17, 3, 25, 1]
  0    1   2   3  4   5  6

Parent: (i - 1) / 2
Left child: 2*i + 1
Right child: 2*i + 2
```

**Операции:**

| Операция | Сложность | Описание |
|----------|-----------|----------|
| Insert | O(log n) | Добавить в конец, sift up |
| Extract Max/Min | O(log n) | Удалить корень, sift down |
| Peek | O(1) | Вернуть корень |
| Heapify (build) | O(n) | Построить из массива |

**Sift Up (после вставки):**
- Сравнить с родителем
- Если нарушает heap property — поменять местами
- Повторять до корня

**Sift Down (после удаления):**
- Заменить корень последним элементом
- Сравнить с детьми, поменять с большим (max-heap)
- Повторять до листа

**Priority Queue — применение:**
- **Dijkstra's Algorithm:** кратчайший путь
- **A* Search:** поиск пути в играх
- **Huffman Coding:** сжатие данных
- **Task Scheduling:** приоритетные задачи
- **Merge K Sorted Lists:** слияние потоков
- **Top K Elements:** k наибольших/наименьших
- **Median in Stream:** два heap (max + min)

**Реализация PQ в JS:**
```javascript
// Упрощенная реализация Min-Heap
class MinHeap {
  constructor() { this.heap = []; }
  
  insert(val) {
    this.heap.push(val);
    this.siftUp(this.heap.length - 1);
  }
  
  extractMin() {
    const min = this.heap[0];
    this.heap[0] = this.heap.pop();
    this.siftDown(0);
    return min;
  }
  
  siftUp(i) { /* ... swap with parent while smaller */ }
  siftDown(i) { /* ... swap with smaller child while larger */ }
}
```

---

### 10. Amortized Analysis

**Вопрос:** Что такое амортизированный анализ? Приведите пример с динамическим массивом.

**Ответ:**

**Амортизированный анализ** — метод оценки средней стоимости операции за последовательность операций, даже если отдельные операции могут быть дорогими.

**Пример: Dynamic Array (ArrayList, vector)**

```
Вставка в конец:
- Обычно O(1) — просто добавить элемент
- При переполнении O(n) — создать новый массив 2x размера, скопировать

Capacity: 4 → 8 → 16 → 32 ...
```

**Анализ:**
```
n вставок, массив удваивается при заполнении:

Копирования происходят при размерах: 1, 2, 4, 8, 16, ... n
Всего копирований: 1 + 2 + 4 + 8 + ... + n = 2n - 1

Общая стоимость n операций:
- n простых вставок: O(n)
- Копирования: O(2n)
- Итого: O(3n)

Амортизированная стоимость одной вставки: O(3n) / n = O(1)
```

**Методы амортизированного анализа:**

**1. Aggregate Method (метод агрегирования):**
- Посчитать общую стоимость n операций
- Разделить на n

**2. Accounting Method (метод бухгалтерского учета):**
- Каждой операции назначить "цену" (может быть > реальной)
- "Сбережения" покрывают дорогие операции

```
Вставка в динамический массив:
- Реальная цена: 1 (или 1 + старый размер при resize)
- Назначенная цена: 3
  - 1 на вставку
  - 1 на будущее копирование этого элемента
  - 1 на копирование старого элемента
```

**3. Potential Method (метод потенциала):**
- Определить функцию потенциала Φ (запасенная энергия)
- Амортизированная стоимость = реальная + ΔΦ

**Другие примеры:**
- **Stack с multipop:** O(1) амортизированно
- **Splay Trees:** O(log n) амортизированно
- **Union-Find:** O(α(n)) почти константа
- **Increment Binary Counter:** O(1) амортизированно

**Важно:** амортизированный анализ — это не среднее по вероятности, а гарантированная средняя стоимость для любой последовательности операций.

---

## 💻 Практика / Live Coding

### 1. LRU Cache

**Задача:** Реализуйте LRU Cache с O(1) сложностью для операций get и put.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                    LRU Cache Structure                       │
│                                                              │
│  HashMap: key → Node                                         │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ "a" → Node1                                             ││
│  │ "b" → Node2                                             ││
│  │ "c" → Node3                                             ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Doubly Linked List (порядок использования):                 │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ HEAD ⟷ [Node3] ⟷ [Node1] ⟷ [Node2] ⟷ TAIL              ││
│  │        (most     (middle)   (least                      ││
│  │        recent)              recent)                     ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Операции:                                                   │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ get(key):                                               ││
│  │   1. Map.get(key) → Node          O(1)                  ││
│  │   2. Переместить Node к HEAD      O(1)                  ││
│  │   3. Вернуть value                                      ││
│  │                                                         ││
│  │ put(key, value):                                        ││
│  │   1. Если key существует:                               ││
│  │      - Обновить value                                   ││
│  │      - Переместить к HEAD                               ││
│  │   2. Если key новый:                                    ││
│  │      - Создать Node, добавить к HEAD                    ││
│  │      - Map.set(key, Node)                               ││
│  │      - Если size > capacity: удалить от TAIL            ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Ключевые компоненты:**
1. **HashMap** для O(1) доступа по ключу
2. **Doubly Linked List** для O(1) перемещения узлов
3. **Dummy HEAD и TAIL** для упрощения операций на границах

**Сложность:** O(1) для get и put.

---

### 2. Longest Palindrome

**Задача:** Напишите функцию, которая находит самый длинный палиндром в строке.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│            Expand Around Center Approach                     │
│                                                              │
│  Строка: "babad"                                             │
│                                                              │
│  Для каждой позиции i:                                       │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 1. Расширяем от центра (нечетная длина):                ││
│  │    [b] a b a d  →  b [a] b a d  →  b a [b] a d          ││
│  │     ↑              ←[a b a]→       ←[b a b a]→?          ││
│  │                                                         ││
│  │ 2. Расширяем между символами (четная длина):            ││
│  │    b|a b a d  →  b a|b a d  →  b a b|a d                ││
│  │     ↑               ↑              ↑                     ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Expand(left, right):                                        │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ while left >= 0 && right < n && s[left] == s[right]:    ││
│  │   left--; right++;                                      ││
│  │ return substring(left+1, right)                         ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Подходы:**

**1. Expand Around Center: O(n²) время, O(1) память**
- Для каждой позиции — расширяем влево и вправо
- Проверяем и нечетные, и четные палиндромы
- Самый практичный подход

**2. Dynamic Programming: O(n²) время, O(n²) память**
- dp[i][j] = true если s[i..j] палиндром
- dp[i][j] = s[i] == s[j] && dp[i+1][j-1]

**3. Manacher's Algorithm: O(n) время, O(n) память**
- Сложный, но оптимальный
- Использует симметрию палиндромов

---

### 3. Two Sum / Three Sum

**Задача:** Решите задачу поиска пар/троек с заданной суммой.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                      TWO SUM                                 │
│                                                              │
│  Вход: nums = [2, 7, 11, 15], target = 9                    │
│                                                              │
│  HashMap approach:                                           │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ complement = target - num                               ││
│  │                                                         ││
│  │ num=2:  complement=7,  map={}      → map[2]=0           ││
│  │ num=7:  complement=2,  map={2:0}   → Found! [0,1]       ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Сложность: O(n) время, O(n) память                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     THREE SUM                                │
│                                                              │
│  Вход: nums = [-1, 0, 1, 2, -1, -4], target = 0             │
│                                                              │
│  Sort + Two Pointers:                                        │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 1. Сортируем: [-4, -1, -1, 0, 1, 2]                     ││
│  │                                                         ││
│  │ 2. Фиксируем i, ищем пару с Two Pointers:               ││
│  │    i=0: nums[i]=-4                                      ││
│  │    [-4, -1, -1, 0, 1, 2]                                ││
│  │      ↑   L              R                                ││
│  │                                                         ││
│  │    sum = -4 + (-1) + 2 = -3 < 0 → L++                   ││
│  │    sum = -4 + (-1) + 2 = -3 < 0 → L++                   ││
│  │    ...                                                  ││
│  │                                                         ││
│  │ 3. Пропускаем дубликаты для i, L, R                     ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Сложность: O(n²) время, O(1) память (не считая сортировку) │
└─────────────────────────────────────────────────────────────┘
```

**Ключевые оптимизации для Three Sum:**
- Сортировка позволяет использовать Two Pointers
- Пропуск дубликатов: `if (i > 0 && nums[i] === nums[i-1]) continue`
- Early termination: если nums[i] > target/3, можно остановиться

---

### 4. Binary Search в Rotated Sorted Array

**Задача:** Реализуйте бинарный поиск для rotated sorted array.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│              Rotated Sorted Array                            │
│                                                              │
│  Исходный: [1, 2, 3, 4, 5, 6, 7]                            │
│  Rotated:  [4, 5, 6, 7, 1, 2, 3]                            │
│                    ↑ pivot point                             │
│                                                              │
│  Свойство: одна из половин ВСЕГДА отсортирована             │
│                                                              │
│  Algorithm:                                                  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ [4, 5, 6, 7, 1, 2, 3]  target = 2                       ││
│  │  L        M        R                                    ││
│  │                                                         ││
│  │ 1. nums[L]=4 <= nums[M]=7 → левая часть отсортирована   ││
│  │                                                         ││
│  │ 2. target=2 в левой части [4..7]? НЕТ                   ││
│  │    → target в правой части → L = M + 1                  ││
│  │                                                         ││
│  │ [4, 5, 6, 7, 1, 2, 3]                                   ││
│  │              L  M  R                                    ││
│  │                                                         ││
│  │ 3. nums[L]=1 <= nums[M]=2 → левая часть отсортирована   ││
│  │                                                         ││
│  │ 4. target=2 в [1..2]? ДА → R = M                        ││
│  │                                                         ││
│  │ ...продолжаем пока L < R                                ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Логика:**
1. Найти mid
2. Определить какая половина отсортирована (сравнить nums[left] с nums[mid])
3. Проверить, находится ли target в отсортированной части
4. Сузить поиск к нужной половине

**Сложность:** O(log n)

**Вариации:**
- С дубликатами: worst case O(n)
- Найти минимум: искать точку перегиба
- Найти pivot index

---

### 5. Tree Traversal без рекурсии

**Задача:** Реализуйте in-order, pre-order, post-order обход дерева итеративно.

**Архитектура решения:**

```
┌─────────────────────────────────────────────────────────────┐
│                    Tree Traversals                           │
│                                                              │
│       1                                                      │
│      / \        Pre-order:  1, 2, 4, 5, 3, 6                │
│     2   3       In-order:   4, 2, 5, 1, 6, 3                │
│    / \   \      Post-order: 4, 5, 2, 6, 3, 1                │
│   4   5   6                                                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  PRE-ORDER (Node, Left, Right)               │
│                                                              │
│  Stack: [1]                                                  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ while stack not empty:                                  ││
│  │   node = stack.pop()                                    ││
│  │   visit(node)                                           ││
│  │   if node.right: stack.push(right)  // сначала right   ││
│  │   if node.left: stack.push(left)    // чтобы left был  ││
│  │                                      // сверху стека   ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  IN-ORDER (Left, Node, Right)                │
│                                                              │
│  Stack: [], current = root                                   │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ while stack not empty OR current != null:               ││
│  │   // Идем влево до упора                                ││
│  │   while current:                                        ││
│  │     stack.push(current)                                 ││
│  │     current = current.left                              ││
│  │                                                         ││
│  │   // Обрабатываем узел                                  ││
│  │   current = stack.pop()                                 ││
│  │   visit(current)                                        ││
│  │                                                         ││
│  │   // Переходим к правому поддереву                      ││
│  │   current = current.right                               ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                POST-ORDER (Left, Right, Node)                │
│                                                              │
│  Подход 1: Два стека                                         │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ stack1 для обхода, stack2 для результата                ││
│  │ Pre-order в обратном порядке детей → reverse = post     ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Подход 2: Один стек + prev pointer                          │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Отслеживаем последний посещенный узел                   ││
│  │ Посещаем node только если:                              ││
│  │   - Нет правого ребенка, ИЛИ                            ││
│  │   - Правый ребенок уже посещен (prev == node.right)     ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Сложность:** O(n) время, O(h) память, где h — высота дерева.

**Morris Traversal:** O(n) время, O(1) память — модифицирует дерево временно.
